# Linear Phase EQ

Flagship linear-phase EQ product workspace.

This folder owns the build script and plugin-specific iteration work for
`Linear Phase EQ`, while still depending on `m4l-builder` for the reusable
framework layer.
